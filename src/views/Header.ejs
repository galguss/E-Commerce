<header>
  <div class="logo">
    <img src="/cart.png" alt="cart-icon" />
    <div class="buttons">
      <% if (isLogged) { %>
      <button class="a" onclick="logout()">logout</button>
      <% } %>
    </div>
  </div>
</header>
